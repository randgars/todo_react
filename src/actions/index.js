import {
  addNote,
  deleteNote,
  setNoteValue,
  setNoteColor,
  setNoteTitle,
  getNotes
} from './note';

const actions = {
  addNote,
  deleteNote,
  setNoteColor,
  setNoteValue,
  setNoteTitle,
  getNotes
};
module.exports = actions;
