import {addNote, deleteNote, setNoteValue, setNoteColor, setNoteTitle} from './note';

const actions = {
  addNote,
  deleteNote,
  setNoteColor,
  setNoteValue,
  setNoteTitle
};
module.exports = actions;
